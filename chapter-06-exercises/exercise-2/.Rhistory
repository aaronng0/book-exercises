# Create a variable `lyric` that contains the text "I like to eat apples and
# bananas"
lyric <- "I like to eat apples and bananas"
# Use the `substr()` function to extract the 1st through 13th letters from the
# `lyric`, and store the result in a variable called `intro`
# Use `?substr` to see more about this function
?substr
# Use the `substr()` function to extract the 1st through 13th letters from the
# `lyric`, and store the result in a variable called `intro`
# Use `?substr` to see more about this function
intro <- substr(lyric, 1, 13)
nchar(lyric)
fruits <- substr(lyric, 15, 32)
fruits <- substr(lyric, 15, nchar(lyric))
fruits <- substr(lyric, 15, nchar(lyric))
?gsub
fruits_e <- gsub("a", "ee", fruits)
fruits_o <- gsub("a", "o", fruits)
lyric_e <- print(intro, fruits_e)
lyric_e <- paste(intro, fruits_e)
print(lyric_e)
print(paste(intro, fruits_o))
say_hello("class")
say_hello <- function(name) {
greeting <- paste("Hello", name)
print(greeting)
}
say_hello("class")
say_hello("Professor Ross")
make_full_name <- function(first_name, last_name) {
full_name <- paste(first_name, last_name)
return(full_name)
}
my_full_name <- make_full_name("Aaron", "Ng")
add_three <- function(number)
{
print(paste(number + 3))
}
add_three(1)
return(number + 3)
add_three <- function(number)
{
return( number + 3)
}
print(ten)
ten <- add_three(7)
print(ten)
dogs <- c("Fido", "Spot", "Sparky")
print(hundred_nums)
hundred_nums <- 1:300
print(hundred_nums)
hundred_nums <- 1:100
(hundred_nums)
v1 <- c(3, 1, 4, 1, 5)
v2 <- c(1, 6, 1, 8, 0)
v3 <- v1 + v2
v4 <- v1 > v2
# Create a vector `names` that contains your name and the names of 2 people
# next to you. Print the vector.
vec <- c("Natalie", "Aaron")
print(vec)
n <- 10:49
print(n)
# Use the `length()` function to get the number of elements in `n`
length(n)
# Add 1 to each element in `n` and print the result
v2 <- c(1)
v3 <- n + v2
# Create a vector `m` that contains the numbers 10 to 1 (in that order).
# Hint: use the `seq()` function
?seq
m <- seq(10, 1)
# Subtract `m` FROM `n`. Note the recycling!
v4 <- m - n
# Subtract `m` FROM `n`. Note the recycling!
v4 <- n - m
x_range <- seq(-5, 10, 0.1)
?sin
sin_wave <- sin(x_range)
cos_wave <- cos(x_range)
wave <- (sin_wave * cos_wave) + sin_wave
?plot
plot(wave)
# Create a vector `words` of 6 (or more) words.
# You can Google for a "random word generator" if you wish!
word <- c(hello, hi, hey, hoi, hola, nihao)
# Create a vector `words` of 6 (or more) words.
# You can Google for a "random word generator" if you wish!
word <- c("hello", "hi", "hey", "hoi", "hola", "nihao")
# Create a vector `words` of 6 (or more) words.
# You can Google for a "random word generator" if you wish!
words <- c("hello", "hi", "hey", "hoi", "hola", "nihao")
print(paste(words + "is the word of the day"))
wordsof <- words + "is the word of the day!"
print(wordsof)
wordsof <- words + "is the word of the day!"
first_ten <- 10:20
next_ten <- seq(21, 30)
all_numbers <- first_ten + next_ten
eleventh <- all_numbers[11]
some_numbers <- all_numbers[2:5]
